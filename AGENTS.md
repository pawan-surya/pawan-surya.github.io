# AGENTS.md

## Project Overview
[Short description of what this repo does]

## Tech Stack
- Language: [e.g. Python / TypeScript /Javascript /HTML/CSS/JQUERY / Go]
- Framework: [e.g. FastAPI / Next.js / Django]
- Database: [e.g. PostgreSQL / MongoDB]

## Setup
- Install dependencies: `pip install -r requirements.txt` or `npm install`
- Run the app: `npm run dev` or `python main.py`

## Testing
- Run all tests: `npm test` or `pytest`
- Always run tests before submitting a PR.

## Code Style
- Follow [PEP8 / ESLint rules / existing patterns in the codebase]
- Write docstrings/comments for all public functions
- Keep functions small and single-purpose

## PR Guidelines
- Write clear commit messages
- One feature or fix per PR
- Never push directly to `main`
